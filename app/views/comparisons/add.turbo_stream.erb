<%= turbo_stream.update "compare_tray", partial: "shared/compare_tray", locals: { compare_limit_alert: flash[:alert] } %>
<%= turbo_stream.replace dom_id(@product, :compare_button), partial: "shared/compare_button", locals: { product: @product } %>
