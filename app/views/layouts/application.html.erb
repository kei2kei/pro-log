<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Pro Log" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Pro Log">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body>
    <div class="js-global-overlay fixed inset-0 z-50 hidden items-center justify-center bg-white/40">
      <div class="shake-loader">
        <%= image_tag "shaker.png", class: "shaker", alt: "Loading" %>
        <p class="shake-text">読み込み中です…</p>
      </div>
    </div>
    <%= render 'shared/header' %>
    <% if flash[:notice].present? || flash[:alert].present? %>
      <div class="bg-white">
        <div class="mx-auto w-full max-w-7xl px-6 pt-6">
          <% if flash[:notice].present? %>
            <div class="rounded-xl border border-emerald-100 bg-emerald-50 px-4 py-3 text-sm text-emerald-800 shadow-sm">
              <%= flash[:notice] %>
            </div>
          <% end %>
          <% if flash[:alert].present? %>
            <div class="mt-3 rounded-xl border border-rose-100 bg-rose-50 px-4 py-3 text-sm text-rose-800 shadow-sm">
              <%= flash[:alert] %>
            </div>
          <% end %>
        </div>
      </div>
    <% end %>
    <%= yield %>
    <%= render 'shared/footer' %>
  </body>
</html>
