<main class="min-h-screen bg-white text-main">
  <section class="border-b border-base bg-white">
    <div class="mx-auto max-w-7xl px-6 py-12">
      <p class="text-xs uppercase tracking-[0.35em] text-brand">Contact</p>
      <h1 class="mt-4 text-4xl font-semibold font-['Fraunces']">お問い合わせ</h1>
      <p class="mt-2 text-sm text-stone-500">ご質問・ご要望は以下の窓口までご連絡ください。</p>
    </div>
  </section>

  <section class="mx-auto max-w-5xl px-6 py-12">
    <div class="grid gap-8 lg:grid-cols-[1fr_1.2fr]">
      <div class="rounded-3xl border border-base bg-white p-8 shadow-sm">
        <div class="space-y-6 text-sm text-stone-700">
          <div>
            <p class="font-semibold text-stone-900">メールでのお問い合わせ</p>
            <p class="mt-2 text-stone-600">内容を確認のうえ、順次ご返信いたします。</p>
          </div>
          <div class="flex flex-wrap items-center gap-3 rounded-2xl border border-base bg-stone-50 px-4 py-3">
            <span class="text-stone-500">Eメールアドレス</span>
            <span class="font-semibold text-brand">support@pro-log.jp</span>
          </div>
          <p class="text-xs text-stone-500">
            返信までにお時間をいただく場合があります。あらかじめご了承ください。
          </p>
        </div>
      </div>

      <div class="rounded-3xl border border-base bg-white p-8 shadow-sm">
        <h2 class="text-lg font-semibold text-stone-900">お問い合わせフォーム</h2>
        <p class="mt-2 text-sm text-stone-500">必要事項をご記入のうえ送信してください。</p>

        <% if @contact_form&.errors&.any? %>
          <div class="mt-6 rounded-2xl border border-rose-100 bg-rose-50 px-4 py-3 text-sm text-rose-700">
            <p class="font-semibold">入力内容をご確認ください。</p>
            <ul class="mt-2 list-disc space-y-1 pl-5">
              <% @contact_form.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <%= form_with model: @contact_form, url: contact_path, method: :post, class: "mt-6 space-y-6", local: true do |f| %>
          <div class="grid gap-4 md:grid-cols-2">
            <div>
              <%= f.label :name, "お名前", class: "text-sm font-semibold text-stone-700" %>
              <%= f.text_field :name, class: "mt-2 w-full rounded-xl border border-base bg-white px-4 py-2.5 text-sm text-stone-900 shadow-sm focus:border-brand focus:outline-none" %>
            </div>
            <div>
              <%= f.label :email, "メールアドレス", class: "text-sm font-semibold text-stone-700" %>
              <%= f.email_field :email, class: "mt-2 w-full rounded-xl border border-base bg-white px-4 py-2.5 text-sm text-stone-900 shadow-sm focus:border-brand focus:outline-none" %>
            </div>
          </div>

          <div>
            <%= f.label :subject, "件名", class: "text-sm font-semibold text-stone-700" %>
            <%= f.text_field :subject, class: "mt-2 w-full rounded-xl border border-base bg-white px-4 py-2.5 text-sm text-stone-900 shadow-sm focus:border-brand focus:outline-none" %>
          </div>

          <div>
            <%= f.label :message, "本文", class: "text-sm font-semibold text-stone-700" %>
            <%= f.text_area :message, rows: 6, class: "mt-2 w-full rounded-2xl border border-base bg-white px-4 py-3 text-sm text-stone-900 shadow-sm focus:border-brand focus:outline-none" %>
          </div>

          <div class="hidden">
            <%= f.label :website, "Webサイト" %>
            <%= f.text_field :website, autocomplete: "off" %>
          </div>

          <div class="flex flex-wrap items-center gap-3">
            <% if ENV["RECAPTCHA_SITE_KEY"].present? %>
              <%= recaptcha_tags %>
            <% else %>
              <p class="text-xs text-stone-500">reCAPTCHAの設定が必要です。</p>
            <% end %>
          </div>

          <div class="flex flex-wrap items-center gap-3">
            <%= f.submit "送信する", class: "btn-primary px-6 py-2.5 text-sm font-semibold" %>
            <p class="text-xs text-stone-500">※内容により返信できない場合があります。</p>
          </div>
        <% end %>
      </div>
    </div>
  </section>
</main>
