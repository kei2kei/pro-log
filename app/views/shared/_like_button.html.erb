<%= button_to likes_path(review_id: review.id), method: :post,
  class: "rounded-full border border-stone-200 px-4 py-2 text-xs font-semibold text-stone-600 hover:border-stone-300",
  form: { data: { loading: "skip" } } do %>
  ♥ <%= t("shared.like.add") %>
<% end %>
