<%= button_to like_path(current_user.review_likes.find_by(review_id: review.id)), method: :delete,
  class: "rounded-full border border-amber-200 bg-amber-50 px-4 py-2 text-xs font-semibold text-amber-600 hover:border-amber-300",
  aria: { label: t("shared.like.saved") },
  form: { data: { loading: "skip" } } do %>
  ♥ <%= t("shared.like.saved") %>
<% end %>
